package umich.simulation

import org.scalatest.FunSuite
import org.scalatest.matchers.ShouldMatchers

class ParameterConfigTest extends FunSuite with ShouldMatchers {
  test("From must be less than To, otherwise you get a NonEmptyList[String] explaining this") {
    pending
  }
}

